console.log(__dirname); // Getting current directory

// The setInterval method calls a function or executes a piece of code at specified intervals 
// (in milliseconds).
// Use 'ctrl + c' to stop the execution of this code below

setInterval(() => {  
    console.log("Hello World")
}, 1000);

// 'clear' can be used to clear the previous code executions on the terminal
// To navigate through your previous commands on the terminal, press the 'up arrow key' or 
// the 'down arrow key'
